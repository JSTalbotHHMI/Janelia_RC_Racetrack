# Firmware Shared

This folder is intended for embedded code shared by multiple sketches.

Good candidates for this area include:

- reusable geometry or patch helpers
- common serial parsing code
- LED animation utilities
- shared hardware abstraction layers

At the moment the firmware code is still organized primarily by experiment, so this folder is ready for future refactoring once common pieces stabilize.
