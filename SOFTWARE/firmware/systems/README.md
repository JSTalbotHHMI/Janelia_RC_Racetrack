# Firmware Systems

This folder is reserved for production-ready or canonical firmware targets.

Use this area when an experiment becomes stable enough that it should be treated as the main supported build for a subsystem. Until then, keep prototypes in `../experiments/`.
