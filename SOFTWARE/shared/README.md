# Shared Software Assets

This folder is intended for software assets shared across multiple tools or runtimes.

Examples:

- protocol notes
- shared data formats
- sample datasets
- common configuration files
- reusable assets that do not belong to one tool alone

The folder is currently empty, but keeping it in the tree makes it easier to promote shared artifacts out of experiment-specific folders later.
